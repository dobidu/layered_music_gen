"""Renderer tests (R-X4): RenderResult assembly + FLUIDSYNTH_VERSION capture.

This file is a Wave 0 skipping stub. Plan 04-02 (Wave 2) replaces the entire
body with real tests. The stub exists so pytest collection succeeds from Wave 0
onward; collection failure breaks pre-commit AST tests that iterate tests/.
"""
import pytest

pytest.skip(
    "Plan 04-00 (Wave 0) scaffold — Plan 04-02 (Wave 2) will replace this stub.",
    allow_module_level=True,
)


def test_placeholder():
    """Placeholder; will be replaced by Plan 04-02's real tests."""
    pass
