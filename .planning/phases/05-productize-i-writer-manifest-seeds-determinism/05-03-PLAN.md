---
phase: 05-productize-i-writer-manifest-seeds-determinism
plan: "03"
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - musicality_score.py
  - src/musicgen/musicality.py
  - music_gen.py
autonomous: true
requirements: [R-P4]
subsystem: package-move
tags: [phase-5, wave-2, git-mv, musicality, package-relocation, d-03]

must_haves:
  truths:
    - "`from musicgen.musicality import get_musicality_score` succeeds and resolves to a callable."
    - "`import musicality_score` raises ModuleNotFoundError (no back-compat shim per D-03 — matches the Plan 03-02 precedent)."
    - "`git log --follow src/musicgen/musicality.py` traces history back through the original repo commit (100% rename detection)."
    - "music_gen.py still imports successfully (the `import musicality_score` reference is rewritten to `from musicgen import musicality`, even though Wave 4 will later delete the entire create_song path that uses it)."
    - "Every other call site / comment reference to `musicality_score` across the repo is updated to `musicgen.musicality` (zero grep hits for `musicality_score` in Python source after this plan, excluding planning directory)."
    - "Zero tests fail — the full suite stays green."
  artifacts:
    - path: src/musicgen/musicality.py
      provides: "MusicalityAnalyzer + get_musicality_score (relocated, byte-identical)"
      contains: "get_musicality_score"
    - path: music_gen.py
      provides: "import site rewritten from musicality_score → musicgen.musicality"
      contains: "from musicgen import musicality"
  key_links:
    - from: music_gen.py (temporary — deleted in Wave 4)
      to: src/musicgen/musicality.py
      via: "`from musicgen import musicality` + `musicality.get_musicality_score(...)`"
      pattern: "from musicgen import musicality"
    - from: git history
      to: src/musicgen/musicality.py
      via: "git mv produces R100 rename status"
      pattern: "R100"
---

<objective>
Wave 2 of Phase 5: relocate `musicality_score.py` into the package as `src/musicgen/musicality.py`.

Purpose: This move has been deferred twice (Phase 3 D-11, Phase 4 D-04). It finally closes here because Wave 4's `api.py` needs a clean `from musicgen import musicality` import — repo-root imports are a code smell once the library has a real public surface (`from musicgen import generate`). By landing the move in its own plan we keep the pattern of 03-02 (one-commit `git mv` + rewrite consumers) and leave Wave 4 free to focus on the orchestrator migration without also debugging import rewrites.

This plan runs in **Wave 2** (parallel with Wave 1's seeds.py — no file overlap, no logical dependency). Wave 3 depends on both Wave 1 and Wave 2 because Wave 3's writer does not touch musicality directly, but Wave 4's api.py does; having musicality in place before Wave 4 starts keeps the dependency graph clean.

Output: 1 file moved via `git mv`, 1 import rewritten in `music_gen.py`, 0 tests broken. Matches Plan 03-02 shape exactly.
</objective>

<execution_context>
@/home/bidu/musicgen/.claude/get-shit-done/workflows/execute-plan.md
@/home/bidu/musicgen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-productize-i-writer-manifest-seeds-determinism/05-CONTEXT.md
@.planning/phases/05-productize-i-writer-manifest-seeds-determinism/05-PATTERNS.md
@.planning/phases/03-package-skeleton-sampler-generators-extraction/03-02-SUMMARY.md
@musicality_score.py
@music_gen.py

<interfaces>
<!-- Current state of the ONE live importer of musicality_score. -->
<!-- Executor rewrites exactly this import in this plan. Wave 4 later deletes the entire create_song() body that uses it. -->

From music_gen.py (current, line 3):
```python
import musicality_score, config
```
and line 120 (inside create_song):
```python
score, component_scores = musicality_score.get_musicality_score(final_wav)  # D-04
```

Target after this plan (music_gen.py lines 3, 120):
```python
# line 3 — split the combined import so the grep for "musicality_score" is clean
import config
from musicgen import musicality
# ...
# line 120 — rewritten call
score, component_scores = musicality.get_musicality_score(final_wav)  # D-04
```

Grep invariant after this plan: `grep -rn "musicality_score" --include="*.py" --exclude-dir=.venv --exclude-dir=__pycache__ --exclude-dir=.planning .` returns zero hits.
</interfaces>

<pattern_references>
Source: `.planning/phases/03-package-skeleton-sampler-generators-extraction/03-02-SUMMARY.md` — the exact precedent. `git mv enhanced_duration_validator.py src/musicgen/duration_validator.py` + rewrite 2 live imports + update 4 docstring/comment references. Zero back-compat shim. This plan is structurally identical with module names swapped.
</pattern_references>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: git mv musicality_score.py → src/musicgen/musicality.py + rewrite music_gen.py import site + sweep stale references</name>
  <files>musicality_score.py, src/musicgen/musicality.py, music_gen.py</files>
  <read_first>
    musicality_score.py (current content — this file is byte-moved, not rewritten),
    music_gen.py (specifically lines 3 and 120 — the only live importer, and the single call site),
    .planning/phases/03-package-skeleton-sampler-generators-extraction/03-02-SUMMARY.md ("Extended the edit scope" section — the executor there had to also update 4 doc-comment references to satisfy the zero-grep-hits acceptance criterion; same pattern applies here),
    .planning/phases/05-productize-i-writer-manifest-seeds-determinism/05-PATTERNS.md (musicality.py section — git mv command verbatim)
  </read_first>
  <action>
    Execute three steps sequentially. Do NOT use `cat`/heredoc/`mv` — use the exact commands below.

    **Step A: git mv the file.** Run from repo root:
    ```bash
    cd /home/bidu/musicgen && git mv musicality_score.py src/musicgen/musicality.py
    ```

    After: `test -f src/musicgen/musicality.py` passes; `test -f musicality_score.py` fails (file is gone, replaced by the new path). `git status --short` shows `R  musicality_score.py -> src/musicgen/musicality.py`.

    **IMPORTANT — no content edits to the moved file.** The move must be a 100% rename (R100 in git) so `git log --follow` preserves history. The moved file becomes byte-identical to the original. If an internal docstring inside `musicality.py` mentions `musicality_score` as its own module name, leave it — those are informational and caught by Step C's sweep if they're in executable position.

    **Step B: rewrite the import site in music_gen.py.** Two edits:

    Edit 1 (line 3): change:
    ```python
    import musicality_score, config
    ```
    to:
    ```python
    import config
    from musicgen import musicality
    ```

    Edit 2 (line 120): change:
    ```python
    score, component_scores = musicality_score.get_musicality_score(final_wav)  # D-04
    ```
    to:
    ```python
    score, component_scores = musicality.get_musicality_score(final_wav)  # D-04
    ```

    These are the ONLY two live references in `music_gen.py` (verified by grep on the current file in the plan's read_first step).

    **Step C: sweep remaining stale references in the tracked tree.**

    Run:
    ```bash
    grep -rn "musicality_score" --include="*.py" --exclude-dir=.venv --exclude-dir=__pycache__ --exclude-dir=.planning /home/bidu/musicgen/
    ```

    For every surviving hit that is NOT a docstring/comment inside `src/musicgen/musicality.py` itself, rewrite:
    - Executable references → `musicality` (the local import name) or `musicgen.musicality` (fully qualified), whichever is syntactically correct.
    - Comment/docstring references → `musicgen.musicality` (the fully qualified path for documentation clarity).

    Expected remaining hits after Step B (to verify with grep BEFORE making edits): zero. The original grep in STATE.md confirmed "The only import site is `music_gen.py:3` (`import musicality_score, config`)." So Step C should be a pure verification step — find zero, commit. If Step C finds surprise hits (e.g. a test that monkeypatched `musicality_score.get_musicality_score`), rewrite them to the new path (D-03's "no back-compat shim" is binding).

    **Post-sweep acceptance invariant:** the grep command above returns zero lines.

    **Regarding the moved file's own internal naming:** if `src/musicgen/musicality.py`'s own module docstring says `"""musicality_score.py — ..."""`, that is a byte-unchanged artifact of the 100% rename. PATTERNS.md's musicality.py section permits leaving file-internal docstring references alone (matches 03-02-SUMMARY's pattern — the 4 docstring/comment references were EXTERNAL to duration_validator.py). If the grep in Step C flags internal-to-musicality.py references and they are in the module-level docstring, edit them separately for clarity; if they are in code body, they are already broken and must be edited. Use judgment.

    **Do NOT delete or create a back-compat shim at the old path.** D-03: no shim. Matches 03-02 precedent (no `enhanced_duration_validator.py` shim exists today).
  </action>
  <verify>
    <automated>cd /home/bidu/musicgen && test -f src/musicgen/musicality.py && ! test -f musicality_score.py && .venv/bin/python -c "from musicgen.musicality import get_musicality_score; print('ok')" && .venv/bin/python -c "import music_gen; print('music_gen imports ok')" && .venv/bin/pytest tests/ -q 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/musicgen/musicality.py` → exit 0
    - `test ! -f musicality_score.py` → exit 0 (old file gone)
    - `git status --short 2>&1 | grep -E "^R  *musicality_score.py -> src/musicgen/musicality.py"` returns 1 line (proves git detected the rename at R100)
    - `.venv/bin/python -c "from musicgen.musicality import get_musicality_score"` exits 0
    - `.venv/bin/python -c "import musicality_score"` exits with ModuleNotFoundError (no back-compat shim; matches D-03)
    - `.venv/bin/python -c "import music_gen"` exits 0 (no import regression — note: may print a FluidSynth-absence WARNING at renderer import, this is expected per Plan 04-05 D-07)
    - `grep -rn "musicality_score" --include="*.py" --exclude-dir=.venv --exclude-dir=__pycache__ --exclude-dir=.planning /home/bidu/musicgen/ | grep -v "src/musicgen/musicality.py:" | wc -l` == 0  (zero stale references in executable code; the moved file's own internal docstring references allowed per 03-02 pattern if in docstring/comment form)
    - `grep -c "from musicgen import musicality" music_gen.py` == 1
    - `grep -c "musicality.get_musicality_score" music_gen.py` == 1
    - `.venv/bin/pytest tests/ -q 2>&1 | tail -1 | grep -c "failed"` == 0  (full suite green)
    - `git log --follow --oneline src/musicgen/musicality.py | head -1` returns 1 line with this plan's commit OR the original upload commit (both valid — proves history preservation)
  </acceptance_criteria>
  <done>
    `musicality_score.py` moved to `src/musicgen/musicality.py` via `git mv` (R100 rename); the single live import site rewritten in `music_gen.py`; zero stale `musicality_score` references in executable Python across the repo; full test suite still green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| old import path → new import path | One-shot rename; no shim means a stale cached `.pyc` or a missed importer surfaces immediately as ModuleNotFoundError. This is desired behavior (D-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | Missed import site | mitigate | Grep sweep in Step C, acceptance criterion requires zero hits across the Python tree |
| T-05-03-02 | Repudiation | git history loss on rename | mitigate | `git mv` command (not `rm + add`) guarantees R100 rename detection; acceptance criterion greps for the R-prefix in status |
| T-05-03-03 | Tampering | Byte-identity of moved file | mitigate | `git mv` performs no content edit; the moved file is exact-copy-then-delete at filesystem level (git status R100) |
</threat_model>

<verification>
`cd /home/bidu/musicgen && git status --short` — shows exactly one renamed file (R-status).
`grep -rn "musicality_score" --include="*.py" --exclude-dir=.venv --exclude-dir=__pycache__ --exclude-dir=.planning .` — zero hits.
`.venv/bin/pytest tests/ -q` — full suite still green.
</verification>

<success_criteria>
- `musicality_score.py` no longer exists at repo root.
- `src/musicgen/musicality.py` exists with history preserved (git log --follow traces back).
- `from musicgen.musicality import get_musicality_score` works.
- `import musicality_score` raises ModuleNotFoundError.
- Full test suite still green (zero regressions from Wave 0's baseline).
- Plan 03-02's pattern is matched exactly (one-commit rename + consumer rewrite, no shim).
</success_criteria>

<output>
After completion, create `.planning/phases/05-productize-i-writer-manifest-seeds-determinism/05-03-SUMMARY.md` — document the rename, the single import rewrite, and any stale-reference sweeps. Note that Wave 4 Plan 05-05 will later delete the `from musicgen import musicality` line and its call site in `music_gen.py` when it deletes create_song; this plan's rewrite is a temporary stepping stone.
</output>
