# Continue Here

**Status:** v0.5.0 COMPLETE — ML-assisted generators shipped + cloud execution wired.
**Latest tag:** `v0.3.0` (pre-v0.4 — main has been progressing past tagged releases since).
**Working branch:** `main` (origin/main up-to-date, commit `3cc34a9`).

## Shipped milestones (newest first)

- **v0.5.0** — Neural chord/melody LSTMs. 4 phases: corpus extractor (`musicgen extract-sequences`), `neural/` package (ChordLSTM ~35 K params, MelodyLSTM ~10 K params, NeuralSampler), generator integration (`Config.chord_backend = "neural"`, `--chord-backend` CLI flag, `functools.lru_cache`-style model loader), docs + notebooks. Optional dep `pip install 'musicgen[neural]'`. Determinism contract preserved.
- **v0.4.1** — Determinism bugfix: `list(set(...))` in `sampler.generate_song_arrangement` produced a PYTHONHASHSEED-dependent part ordering. Fixed with `sorted(set(...))`. SHA-256 goldens regenerated.
- **v0.4.0** — Sample composition (M3 / M4 / M5): real audio sample mixing alongside / substituting FluidSynth layers. `musicgen samples build` library builder, `used_samples` annotation, `--sample-db` CLI flags. `musicality` extracted as standalone installable package (M1).
- **v0.3.0** — Higher-order Markov chord/melody + two-layer musicality quality gate + calibration harness.
- **v0.2.0** — 8 built-in genres, `GenreSpec` composition engine, extended chord vocabulary, genre-constrained sampler / FX / soundfont selection.
- **v0.1.0** — first shippable dataset generator. PR #2 merged.

## Current state of the repo

- **Tests:** 1273 fast passing (`pytest -m "not slow"`). 3 pre-existing flaky batch tests under parallelism (pass in isolation). Slow FluidSynth-gated tests guarded by `pytest -m slow`.
- **Public API:** `from musicgen import generate, generate_batch, Config, SampleResult, BatchResult, __version__`.
- **CLI:** `musicgen generate / clean / calibrate / list-genres / index-midi / index-audio / samples build / extract-sequences / train`.
- **Optional extras:** `musicgen[samples]` (audio-sample-manager + soundfile + rubberband-stretch) · `musicgen[neural]` (torch >= 2.0) · `musicgen[dev]`.
- **Docs:** `README.md`, `docs/musicality-scoring.md`, `docs/sample-composition.md`, `docs/neural-generators.md`, `genres/README.md`.
- **Notebooks:** `notebooks/{musicgen_demo, sample_composition, neural_generators}.ipynb` (each with a Colab/Binder bootstrap cell).
- **Cloud execution:** Colab badges in README, `binder/` (apt.txt + postBuild + requirements.txt), `hf_space/` (Dockerfile + Gradio app.py + Space metadata README).

## Outstanding optional items

- Re-capture SHA-256 determinism goldens on a FluidSynth-equipped host now that pyproject version is `0.5.0` — older goldens were captured against `0.1.0`:
  ```
  pip install -e ".[dev]"
  pytest -m slow --regen-goldens tests/test_determinism_golden.py
  git add tests/fixtures/determinism/
  git commit -m "test: re-capture determinism goldens for v0.5.0"
  ```
- Push `hf_space/` to a real HuggingFace Space to activate path 3 of cloud execution.
- Tag `v0.5.0` (current `main` HEAD is `3cc34a9`).

## Possible next milestones

- **v0.6 — Public release prep:** soundfont license audit + CC0/MIT replacement, HuggingFace Datasets exporter, sharded directory layout for 100k+ samples.
- **v0.6 alt — Larger neural backends:** Transformer chord/melody, training on real public MIDI corpora (Lakh, MAESTRO, MetaMIDI), pretrained checkpoints shipped under `models/`.

## Resume instructions

1. Read this file.
2. Read `.planning/STATE.md` frontmatter for milestone status.
3. `git log --oneline -10` for the most recent commits.
4. Check tests are still green: `pytest -m "not slow" -q`.
